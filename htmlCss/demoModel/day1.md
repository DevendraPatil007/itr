pratice of day 1

github and vs code installtion
connection of both through git command
the command are
git add . :- to select the file
git status :- to check the branch
git commit -m :- description of the commit /or the title
git push -u {origin} git branchName :- used to push the code to github
from given source to its branch/main
git
git checkout -b newBranchName :- used to switch on new made branch
git branch :- to check the current branch

this is done in branch 2
